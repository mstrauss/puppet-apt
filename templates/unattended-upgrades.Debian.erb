// file managed by puppet
Unattended-Upgrade::Allowed-Origins {
        "Debian <%= lsbdistcodename == 'lenny' ? 'oldstable' : 'stable' %>";
<% if lsbdistcodename == 'lenny' -%>        "Debian-volatile oldstable";<% end %>
};
